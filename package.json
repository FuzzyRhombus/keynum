{
  "name": "keynum",
  "version": "1.0.0",
  "description": "A simple enum type with emphasis on keys",
  "main": "enum.js",
  "scripts": {
    "test": "jshint enum.js && tape test/*.test.js | tap-spec"
  },
  "keywords": [
    "enum",
    "key",
    "value",
    "string"
  ],
  "author": "Ryan Mastrolia <ryan.mastrolia@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "jshint": "^2.9.4",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.2"
  }
}
